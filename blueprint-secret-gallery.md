# 비밀번호 보호 사진 갤러리 웹앱 설계서

> 작성일: 2026-03-17
> 목적: Claude Code 구현 참조용 계획서

---

## 1. 작업 컨텍스트

### 배경 및 목적
아이폰에서 특정 사진을 비밀번호로 보호하여 별도로 관리할 수 있는 웹앱이 필요하다. HTML 단일 파일로 제작하여 아이폰 홈 화면에 추가하면 앱처럼 작동하며, 별도의 앱스토어 배포나 Mac/Xcode 없이 사용 가능하다.

### 범위
- 포함: 비밀번호 설정/변경/잠금, 사진 추가(촬영 또는 갤러리 선택), 사진 보기/삭제, 로컬 저장
- 제외: 클라우드 동기화, 앨범/폴더 분류, 사진 편집, 공유 기능

### 입출력 정의

| 항목 | 내용 |
|------|------|
| **입력** | 사용자가 촬영하거나 갤러리에서 선택한 사진 파일 (JPEG, PNG, HEIC) |
| **출력** | IndexedDB에 암호화 저장된 사진 데이터, 썸네일 그리드 UI |
| **트리거** | 사용자가 HTML 파일을 열거나 홈 화면 아이콘 탭 |

### 제약조건
- 기술적 제약: HTML 단일 파일, 외부 서버 없음, IndexedDB 저장 (브라우저별 약 50~100MB 제한)
- 운영 제약: 브라우저 캐시/데이터 삭제 시 사진 소실 가능
- 품질 제약: 썸네일 생성 시 Canvas API 활용, 원본 보존

### 용어 정의
| 용어 | 정의 |
|------|------|
| PWA | Progressive Web App — 웹 기술로 만든 앱처럼 작동하는 웹사이트 |
| IndexedDB | 브라우저 내장 로컬 데이터베이스, 대용량 바이너리 저장 가능 |
| 썸네일 | 갤러리 목록에 표시되는 축소 이미지 |

---

## 2. 워크플로우 정의

### 전체 흐름도

```
[앱 실행] → [비밀번호 확인] → [갤러리 메인]
                ↓                    ↓
          [최초 실행?]          [사진 추가] → [촬영/선택] → [썸네일 생성] → [IndexedDB 저장]
           ↙       ↘           [사진 보기] → [전체화면 뷰어]
   [비번 설정]  [비번 입력]      [사진 삭제] → [확인] → [IndexedDB 삭제]
                                [비번 변경] → [현재 비번 확인] → [새 비번 설정]
```

### LLM 판단 vs 코드 처리 구분

| LLM이 직접 수행 | 스크립트로 처리 |
|----------------|----------------|
| 없음 (순수 프론트엔드 앱) | 비밀번호 해싱/검증, 사진 읽기/저장/삭제, 썸네일 생성, UI 렌더링 |

### 단계별 상세

#### Step 1: 앱 진입 — 비밀번호 확인

- **처리 주체**: 코드 (JavaScript)
- **입력**: 앱 실행 이벤트
- **처리 내용**:
  1. IndexedDB에서 저장된 비밀번호 해시 존재 여부 확인
  2. 최초 실행이면 → 비밀번호 설정 화면 표시 (4~20자, 확인 입력 포함)
  3. 기존 사용자면 → 비밀번호 입력 화면 표시
  4. 입력된 비밀번호를 SHA-256 해싱하여 저장된 해시와 비교
  5. 5회 연속 실패 시 30초 잠금
- **출력**: 인증 성공 → 갤러리 메인 화면 전환
- **성공 기준**: 올바른 비밀번호 입력 시 갤러리 진입, 틀린 비밀번호 시 오류 메시지
- **검증 방법**: 규칙 기반 (해시 일치 여부)
- **실패 시 처리**: 오류 메시지 표시 + 재시도 카운트 증가

#### Step 2: 사진 추가

- **처리 주체**: 코드 (JavaScript)
- **입력**: 사용자가 선택한 사진 파일 또는 카메라 촬영
- **처리 내용**:
  1. `<input type="file" accept="image/*" capture="environment">` 로 사진 촬영/선택
  2. FileReader로 Base64 또는 ArrayBuffer 변환
  3. Canvas API로 썸네일 생성 (200x200 크기 조정)
  4. 원본 + 썸네일을 IndexedDB에 저장 (키: timestamp + 랜덤ID)
  5. 갤러리 그리드 새로고침
- **출력**: IndexedDB에 저장된 사진 레코드 `{ id, original, thumbnail, addedAt }`
- **성공 기준**: 사진이 갤러리 그리드에 즉시 표시됨
- **검증 방법**: 규칙 기반 (IndexedDB 트랜잭션 성공 콜백)
- **실패 시 처리**: 저장 실패 시 "저장 공간이 부족합니다" 알림

#### Step 3: 사진 보기 (전체화면 뷰어)

- **처리 주체**: 코드 (JavaScript)
- **입력**: 갤러리 그리드에서 썸네일 탭
- **처리 내용**:
  1. IndexedDB에서 원본 이미지 로드
  2. 전체화면 오버레이로 표시
  3. 좌우 스와이프로 이전/다음 사진 이동
  4. 핀치 줌 지원
- **출력**: 전체화면 이미지 뷰어 UI
- **성공 기준**: 원본 해상도로 표시, 스와이프/줌 작동
- **검증 방법**: 규칙 기반 (이미지 로드 완료 이벤트)
- **실패 시 처리**: 이미지 로드 실패 시 "사진을 불러올 수 없습니다" 표시

#### Step 4: 사진 삭제

- **처리 주체**: 코드 (JavaScript)
- **입력**: 사용자의 삭제 요청 (길게 누르기 또는 삭제 버튼)
- **처리 내용**:
  1. 삭제 확인 다이얼로그 표시
  2. 확인 시 IndexedDB에서 해당 레코드 삭제
  3. 갤러리 그리드 새로고침
- **출력**: 삭제된 사진이 갤러리에서 제거됨
- **성공 기준**: 삭제 후 해당 사진이 그리드에서 사라짐
- **검증 방법**: 규칙 기반 (IndexedDB 삭제 트랜잭션 성공)
- **실패 시 처리**: 실패 시 "삭제에 실패했습니다" 알림

#### Step 5: 비밀번호 변경

- **처리 주체**: 코드 (JavaScript)
- **입력**: 설정 메뉴에서 비밀번호 변경 요청
- **처리 내용**:
  1. 현재 비밀번호 입력 확인
  2. 새 비밀번호 입력 + 확인 입력
  3. SHA-256 해싱 후 IndexedDB에 저장
- **출력**: 업데이트된 비밀번호 해시
- **성공 기준**: 새 비밀번호로 로그인 가능
- **검증 방법**: 규칙 기반 (해시 저장 성공)
- **실패 시 처리**: 현재 비밀번호 불일치 시 오류 메시지

### 상태 전이

| 상태 | 전이 조건 | 다음 상태 |
|------|----------|----------|
| 앱 시작 | 비밀번호 미설정 | 비밀번호 설정 |
| 앱 시작 | 비밀번호 설정됨 | 비밀번호 입력 |
| 비밀번호 설정 | 비밀번호 등록 완료 | 갤러리 메인 |
| 비밀번호 입력 | 인증 성공 | 갤러리 메인 |
| 비밀번호 입력 | 5회 실패 | 30초 잠금 |
| 갤러리 메인 | 사진 탭 | 전체화면 뷰어 |
| 갤러리 메인 | + 버튼 탭 | 사진 추가 |
| 갤러리 메인 | 설정 탭 | 설정 메뉴 |

---

## 3. 구현 스펙

### 파일 구조

```
바탕화면/
  └── 비밀갤러리.html          # 단일 HTML 파일 (CSS + JS 인라인)
```

### 기술 스택

| 항목 | 기술 |
|------|------|
| UI 프레임워크 | 순수 HTML/CSS/JavaScript (프레임워크 없음) |
| 데이터 저장 | IndexedDB (idb-keyval 패턴 직접 구현) |
| 비밀번호 해싱 | Web Crypto API (SHA-256) |
| 썸네일 생성 | Canvas API |
| 이미지 뷰어 | CSS transform + touch 이벤트 (스와이프/줌) |
| PWA 지원 | `<meta name="apple-mobile-web-app-capable">` + manifest 인라인 |

### UI 화면 구성

| 화면 | 설명 |
|------|------|
| 잠금 화면 | 비밀번호 입력 필드, 숫자/문자 키패드, 로고 |
| 비밀번호 설정 | 새 비밀번호 입력 + 확인 입력, 등록 버튼 |
| 갤러리 메인 | 사진 썸네일 그리드 (3열), 하단 + 버튼, 상단 설정 아이콘 |
| 전체화면 뷰어 | 원본 이미지 표시, 좌우 스와이프, 핀치 줌, 닫기/삭제 버튼 |
| 설정 | 비밀번호 변경, 저장 용량 확인, 전체 삭제 |

### 모바일 최적화

- `viewport` 메타 태그로 모바일 최적화
- `apple-mobile-web-app-capable` 메타 태그로 전체화면 모드
- `apple-mobile-web-app-status-bar-style` 메타 태그로 상태바 스타일
- CSS `safe-area-inset` 적용 (노치 대응)
- 터치 이벤트 기반 스와이프/줌 (마우스 이벤트 폴백 불필요)
- `user-select: none` 으로 실수 선택 방지

### 보안 고려사항

| 항목 | 구현 방법 |
|------|----------|
| 비밀번호 저장 | SHA-256 해시 + salt, 원본 비밀번호 저장하지 않음 |
| 잠금 시도 제한 | 5회 실패 시 30초 잠금, 카운트는 메모리에만 보관 |
| 세션 관리 | 앱 전환/백그라운드 시 자동 잠금 (visibilitychange 이벤트) |
| 데이터 보호 | IndexedDB 데이터는 같은 origin에서만 접근 가능 |

### 주요 산출물 파일

| 파일 | 형식 | 생성 단계 | 용도 |
|------|------|----------|------|
| `비밀갤러리.html` | HTML | 구현 완료 시 | 아이폰에서 직접 실행하는 단일 파일 웹앱 |

---

## 4. 사용 방법

1. `비밀갤러리.html` 파일을 아이폰으로 전송 (메일, 카카오톡, AirDrop 등)
2. 아이폰에서 파일을 Safari로 열기
3. Safari 공유 버튼 → "홈 화면에 추가" 탭
4. 홈 화면 아이콘으로 앱처럼 실행
5. 최초 실행 시 비밀번호 설정 → 이후 사진 추가/관리
