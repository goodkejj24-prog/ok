# 왁싱샵 고객관리 시스템 설계서

> 작성일: 2026-04-01
> 목적: Claude Code 구현 참조용 계획서

---

## 1. 작업 컨텍스트

### 배경 및 목적
미용샵(왁싱 전문) 1인 운영자를 위한 고객관리 시스템. 고객 등록부터 예약, 시술 완료, 사후관리까지의 흐름을 추적하고, 상황별로 보내야 할 안내 문자를 대시보드에서 확인 후 수동 발송할 수 있도록 한다.

### 범위
- 포함: 고객 등록, 예약 관리, 시술 완료 기록, 상황별 문자 안내 대시보드, 문자 복사 기능
- 제외: 실제 SMS API 자동 발송, 결제/매출 관리, 다중 사용자 권한

### 입출력 정의

| 항목 | 내용 |
|------|------|
| **입력** | 고객 정보(이름, 연락처, 메모), 예약 정보(날짜, 시간, 시술 부위), 시술 완료 처리 |
| **출력** | 대시보드(문자 발송 대상 목록), 복사 가능한 문자 메시지, 고객 리스트/이력 |
| **트리거** | 사용자가 핸드폰 브라우저에서 앱 접속 |

### 제약조건
- 1인 사용 (인증/로그인 불필요, 단 기기 접근으로 보안 대체)
- 모바일 최적화 필수 (PWA로 홈화면 설치 가능)
- 로컬 DB 사용 (IndexedDB 또는 localStorage) — 서버 없이 핸드폰에서 독립 작동
- 데이터 백업: JSON 내보내기/가져오기 기능으로 수동 백업

### 용어 정의
| 용어 | 정의 |
|------|------|
| 신규고객 | 처음 등록된 고객 (아직 예약/시술 이력 없음) |
| 예약확정 | 고객의 첫 예약이 등록된 상태 |
| 시술완료 | 해당 예약의 시술이 끝난 상태 |
| 리마인더 대상 | 마지막 시술일로부터 4주 경과 고객 (5주차 예약 안내 시점) |

---

## 2. 워크플로우 정의

### 전체 흐름도

```
[고객 등록] → [예약 등록] → [예약확정 문자 안내]
                               ↓
                          [시술 완료 처리]
                               ↓
                    [시술 후 주의사항 문자 안내]
                               ↓
                    [4주 경과 시 리마인더 문자 안내]
                               ↓
                          [재예약] → (반복)
```

### 대시보드 문자 안내 분류

| 구분 | 조건 | 문자 내용 |
|------|------|----------|
| 예약확정 안내 | 예약이 등록되고 아직 문자 미발송 | 예약 확인 + 노쇼 비용 안내 |
| 시술 후 안내 | 시술 완료 처리되고 아직 문자 미발송 | 왁싱 후 주의사항 |
| 재예약 리마인더 | 마지막 시술일로부터 4주 경과 + 새 예약 없음 | 5주차 재예약 안내 |

### 문자 템플릿

#### 1) 예약확정 안내 문자
```
{고객명}님 안녕하세요 :)
{예약일} {예약시간}에 왁싱 예약이 확정되었습니다.

혹시 일정 변경이 필요하시면 하루 전까지 연락 부탁드려요.
원활한 예약 운영을 위해 당일 노쇼 시 노쇼비 2만원이 발생하는 점 양해 부탁드립니다.

그럼 당일 뵙겠습니다!
- Eunjeong
```

#### 2) 시술 후 주의사항 문자
```
{고객명}님, 오늘 시술 감사합니다 :)
왁싱 후 주의사항 간단히 안내드려요!

- 24시간 내 뜨거운 물(사우나, 반신욕) 삼가주세요
- 시술 부위 꽉 끼는 옷 피해주세요
- 2~3일간 격한 운동, 수영장 이용 삼가주세요
- 인그로운 헤어 방지를 위해 3일 후부터 부드럽게 각질 관리해주세요
- 보습제 꾸준히 발라주시면 좋아요!

궁금한 점 있으시면 편하게 연락주세요 :)
- Eunjeong
```

#### 3) 재예약 리마인더 문자
```
{고객명}님 안녕하세요 :)
지난 시술 후 벌써 한 달이 지났네요!

왁싱은 약 5주 주기로 관리해주시면 더 깔끔한 결과를 유지하실 수 있어요.
편하신 시간에 예약 잡아주시면 감사하겠습니다!

- Eunjeong
```

### LLM 판단 vs 코드 처리 구분

| LLM이 직접 수행 | 스크립트로 처리 |
|----------------|----------------|
| 해당 없음 (이 시스템은 LLM 불필요) | 고객 CRUD, 예약 CRUD, 날짜 계산, 문자 대상 필터링, UI 렌더링 |

### 단계별 상세

#### Step 1: 고객 등록

- **처리 주체**: 앱 UI + IndexedDB
- **입력**: 이름, 연락처, 메모(선택)
- **처리 내용**: 고객 정보를 DB에 저장
- **출력**: 고객 목록에 추가
- **성공 기준**: 고객이 목록에 표시됨
- **검증 방법**: 필수 필드(이름, 연락처) 입력 확인
- **실패 시 처리**: 필수 필드 미입력 시 입력 요청 안내

#### Step 2: 예약 등록

- **처리 주체**: 앱 UI + IndexedDB
- **입력**: 고객 선택, 예약일시, 시술 부위
- **처리 내용**: 예약 정보 저장, 상태를 "예약확정"으로 설정, 문자 발송 상태를 "미발송"으로 설정
- **출력**: 대시보드 "예약확정 안내" 섹션에 표시
- **성공 기준**: 예약이 등록되고 대시보드에 문자 대상으로 노출
- **검증 방법**: 필수 필드(고객, 날짜) 입력 확인
- **실패 시 처리**: 필수 필드 미입력 시 입력 요청 안내

#### Step 3: 문자 확인 및 발송 처리

- **처리 주체**: 앱 UI
- **입력**: 대시보드에서 문자 대상 선택
- **처리 내용**: 템플릿에 고객명/날짜 자동 삽입 → 문자 내용 표시 → "복사" 버튼 클릭 → 클립보드 복사 → 사용자가 문자앱에서 수동 발송 → "발송 완료" 버튼으로 상태 업데이트
- **출력**: 해당 문자 상태가 "발송완료"로 변경, 대시보드에서 제거
- **성공 기준**: 문자 상태가 "발송완료"로 업데이트됨
- **검증 방법**: 상태 변경 확인
- **실패 시 처리**: 해당 없음

#### Step 4: 시술 완료 처리

- **처리 주체**: 앱 UI + IndexedDB
- **입력**: 예약 목록에서 해당 예약 선택
- **처리 내용**: 예약 상태를 "시술완료"로 변경, 시술 완료일 기록, "시술 후 안내" 문자 대상으로 등록
- **출력**: 대시보드 "시술 후 안내" 섹션에 표시, 4주 후 리마인더 자동 예약
- **성공 기준**: 상태 변경 + 대시보드 반영
- **검증 방법**: 상태 확인
- **실패 시 처리**: 해당 없음

#### Step 5: 리마인더 체크 (앱 접속 시 자동)

- **처리 주체**: 앱 로직 (접속 시 자동 실행)
- **입력**: 전체 시술 완료 고객 목록
- **처리 내용**: 마지막 시술일로부터 28일(4주) 이상 경과 + 새 예약 없는 고객 필터링
- **출력**: 대시보드 "재예약 리마인더" 섹션에 표시
- **성공 기준**: 해당 고객이 대시보드에 노출
- **검증 방법**: 날짜 계산 로직 검증
- **실패 시 처리**: 해당 없음

### 상태 전이

| 상태 | 전이 조건 | 다음 상태 |
|------|----------|----------|
| 신규고객 | 예약 등록 | 예약확정 |
| 예약확정 | 시술 완료 버튼 | 시술완료 |
| 시술완료 | 4주 경과 + 새 예약 없음 | 리마인더 대상 |
| 리마인더 대상 | 새 예약 등록 | 예약확정 (반복) |

---

## 3. 구현 스펙

### 기술 스택

| 항목 | 선택 | 이유 |
|------|------|------|
| 프레임워크 | Next.js (현재 프로젝트) | 기존 프로젝트 활용 |
| UI | Tailwind CSS + shadcn/ui | 이미 설치됨, 모바일 최적화 용이 |
| DB | IndexedDB (Dexie.js 라이브러리) | 서버 없이 브라우저 내 영구 저장 |
| 배포 | Vercel (무료) | Next.js 기본 배포 플랫폼 |
| 설치 | PWA (홈화면 추가) | 앱처럼 사용 가능 |

### 폴더 구조

```
/project-root
  ├── CLAUDE.md
  ├── next.config.ts
  ├── public/
  │   ├── manifest.json              # PWA 매니페스트
  │   └── icons/                     # PWA 아이콘
  ├── src/
  │   ├── app/
  │   │   ├── layout.tsx
  │   │   ├── page.tsx               # 대시보드 (메인)
  │   │   ├── customers/
  │   │   │   └── page.tsx           # 고객 목록/등록
  │   │   ├── reservations/
  │   │   │   └── page.tsx           # 예약 관리
  │   │   └── settings/
  │   │       └── page.tsx           # 데이터 백업/복원
  │   ├── components/
  │   │   ├── Dashboard.tsx          # 메인 대시보드 (문자 대상 3개 섹션)
  │   │   ├── CustomerForm.tsx       # 고객 등록/수정 폼
  │   │   ├── CustomerList.tsx       # 고객 목록
  │   │   ├── ReservationForm.tsx    # 예약 등록/수정 폼
  │   │   ├── ReservationList.tsx    # 예약 목록
  │   │   ├── MessagePreview.tsx     # 문자 미리보기 + 복사 버튼
  │   │   ├── BottomNav.tsx          # 하단 네비게이션 바
  │   │   └── ui/                    # shadcn/ui 컴포넌트
  │   ├── lib/
  │   │   ├── db.ts                  # Dexie.js DB 스키마 및 초기화
  │   │   ├── types.ts               # TypeScript 타입 정의
  │   │   ├── message-templates.ts   # 문자 템플릿 3종
  │   │   ├── date-utils.ts          # 날짜 계산 유틸 (4주 경과 체크 등)
  │   │   └── backup.ts              # JSON 내보내기/가져오기
  │   └── hooks/
  │       ├── useCustomers.ts        # 고객 CRUD 훅
  │       ├── useReservations.ts     # 예약 CRUD 훅
  │       └── useDashboard.ts        # 대시보드 문자 대상 필터링 훅
  └── output/                        # (사용 안 함)
```

### 데이터 모델

#### Customer (고객)
```typescript
{
  id: string;              // UUID
  name: string;            // 고객명
  phone: string;           // 연락처
  memo?: string;           // 메모
  createdAt: Date;         // 등록일
}
```

#### Reservation (예약)
```typescript
{
  id: string;              // UUID
  customerId: string;      // 고객 ID (FK)
  date: string;            // 예약일 (YYYY-MM-DD)
  time: string;            // 예약 시간 (HH:mm)
  area: string;            // 시술 부위
  status: 'reserved' | 'completed' | 'cancelled' | 'noshow';
  completedAt?: Date;      // 시술 완료일
  messages: {
    booking: 'pending' | 'sent';     // 예약확정 문자
    aftercare: 'pending' | 'sent' | 'na';  // 시술 후 안내 문자
    reminder: 'pending' | 'sent' | 'na';   // 리마인더 문자
  };
  createdAt: Date;
}
```

### 화면 구성

#### 1) 대시보드 (메인 화면 `/`)
- **예약확정 안내** 카드 목록: 예약 등록 후 문자 미발송 건
- **시술 후 안내** 카드 목록: 시술 완료 후 문자 미발송 건
- **재예약 리마인더** 카드 목록: 4주 경과 고객
- 각 카드: 고객명, 연락처, "문자 보기" 버튼 → MessagePreview 팝업
- MessagePreview: 문자 내용 표시 + "복사" 버튼 + "발송 완료" 버튼

#### 2) 고객 관리 (`/customers`)
- 고객 목록 (검색 가능)
- 신규 등록 버튼 → CustomerForm
- 고객 탭하면 상세 (시술 이력 포함)

#### 3) 예약 관리 (`/reservations`)
- 예약 목록 (날짜순, 상태 필터)
- 신규 예약 버튼 → ReservationForm (고객 선택 드롭다운)
- "시술 완료" 버튼으로 상태 변경

#### 4) 설정 (`/settings`)
- 데이터 내보내기 (JSON 다운로드)
- 데이터 가져오기 (JSON 업로드)

#### 하단 네비게이션
```
[ 대시보드 ] [ 고객 ] [ 예약 ] [ 설정 ]
```

### 주요 산출물 파일

| 파일 | 형식 | 생성 단계 | 용도 |
|------|------|----------|------|
| `src/lib/db.ts` | TypeScript | 초기 구현 | IndexedDB 스키마 및 Dexie 설정 |
| `src/lib/message-templates.ts` | TypeScript | 초기 구현 | 문자 템플릿 3종 (변수 치환 함수) |
| `public/manifest.json` | JSON | 초기 구현 | PWA 설치용 매니페스트 |
| 백업 파일 | JSON | 사용자 수동 | 전체 데이터 백업/복원용 |

---

## 4. 구현 우선순위

| 순서 | 항목 | 설명 |
|------|------|------|
| 1 | DB + 타입 정의 | Dexie.js 설정, Customer/Reservation 모델 |
| 2 | 고객 등록/목록 | 기본 CRUD |
| 3 | 예약 등록/목록 | 예약 CRUD + 시술완료 처리 |
| 4 | 대시보드 | 문자 대상 필터링 + 문자 미리보기/복사 |
| 5 | PWA 설정 | manifest.json, 서비스워커, 아이콘 |
| 6 | 설정(백업/복원) | JSON 내보내기/가져오기 |
| 7 | 모바일 UI 다듬기 | 터치 최적화, 애니메이션, UX 개선 |
